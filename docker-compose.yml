version: '3.8'
services:
  server:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    environment:
      PORT: 3000
    restart: unless-stopped
    volumes:
      - /var/run/usbmuxd:/var/run/usbmuxd
